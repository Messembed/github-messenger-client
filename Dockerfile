FROM node:14

WORKDIR /usr/src/app

COPY package.json yarn.lock ./

RUN yarn

ARG MESSEMBED_URL
ARG GITHUB_MESSENGER_BACKEND_URL

ENV MESSEMBED_URL $MESSEMBED_URL
ENV GITHUB_MESSENGER_BACKEND_URL $GITHUB_MESSENGER_BACKEND_URL

COPY . .

RUN yarn build

CMD yarn start
